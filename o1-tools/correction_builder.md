The following app is not working as expected:

```app

```

```bug

```

Debug this app.

Return the *full* content of the `.R` files that need to be modified.

Do not return the `.R` files that do not require modifications.